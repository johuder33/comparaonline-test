const chai = require('chai');
const { expect } = chai;
// import CarInsurance class

describe('Suite Tests CarInsurance', () => {
    it('should fail', () => {
        expect('should fails').to.equal('should fails');
    });
})